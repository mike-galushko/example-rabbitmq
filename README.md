# example-rabbitmq
Practicing with RabbitMQ

  ```text
  docker save -o example_rabbit_api.tar example_rabbit_api
  ```

1. Перенести файл на PROD и развернуть в Docker

  ```text
  docker load < example_rabbit_api.tar

  ```
